<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Posts</title>
  </head>
  <body>
    <h1>All Posts</h1>
    <ul>
      <% @posts.each do |t| %>
      <li><%= t.name %></li>
      <ul><%= t.content %></ul>
      <% end %>
    </ul>
  </body>
</html>
